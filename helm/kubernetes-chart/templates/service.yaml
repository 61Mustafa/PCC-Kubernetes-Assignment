apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-chart.fullname" . }}
  labels:
    {{- include "kubernetes-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernetes-chart.selectorLabels" . | nindent 4 }}
